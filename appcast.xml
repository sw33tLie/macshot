<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle">
    <channel>
        <title>macshot</title>
        <link>https://github.com/sw33tLie/macshot</link>
        <description>macshot updates</description>
<item>
    <title>v4.1.0</title>
    <pubDate>Fri, 08 May 2026 21:55:31 +0000</pubDate>
    <sparkle:version>73</sparkle:version>
    <sparkle:shortVersionString>4.1.0</sparkle:shortVersionString>
    <sparkle:minimumSystemVersion>12.3</sparkle:minimumSystemVersion>

    <description><![CDATA[
      <style>
        body { font-family: -apple-system, sans-serif; font-size: 13px; padding: 12px; }
        h3 { font-size: 14px; margin: 12px 0 6px 0; }
        h3:first-child { margin-top: 0; }
        ul { padding-left: 18px; margin: 4px 0; }
        li { margin: 4px 0; line-height: 1.5; }
      </style>
       The big release in 4.1.0 is a full **video editor effects suite**: zoom, censor, cut, speed, freeze, and text segments rendered through a custom Core Image compositor. Recording quality, color reproduction across external monitors, and capture-flow ergonomics also got significant attention.  </ul><h3>Added</h3><ul>  <li><b>Video editor effects suite</b> — six segment types you can stack on the timeline:</li>     - **Zoom** segments with direct-manipulation rect on the preview, fade in/out, 1.2×–5× range.     - **Censor** segments — solid / pixelate / blur a rectangular region for a time range. Two censors can overlap.     - **Cut** segments — physically remove a range of frames from the export.     - **Speed** segments — retime a range at 0.25×–10×, audio stays in sync.     - **Freeze** segments — hold a single source frame for a chosen duration.     - **Text** segments — overlay styled text (size, bold/italic, color, background pill, alignment, fade) anywhere on the video, with double-click to edit in place. <li><b>Multi-row effects band</b> — overlapping segments stack into separate rows; up to 4 rows visible, scrolls vertically beyond that.</li> <li><b>Custom video compositor</b> — single Core Image render path shared by live preview, MP4 export, and GIF export. Smooth motion, filter-based effects (blur/pixelate) alongside transform-based zoom.</li> <li><b>Video export quality presets</b> (Low / Medium / High) and **export dimensions dropdown** (Original, 75%, 50%, 33%, 25%) with estimated output size.</li> <li><b>Right-click to anchor capture rect</b> — right-click an empty overlay to start a selection, then move the cursor without holding any button.</li> <li><b>Capture Last Area</b> — re-capture the previous selection on demand from the menu bar, hotkey, or `macshot://capture-last`.</li> <li><b>URL scheme for external tools</b> — `macshot://capture`, `macshot://ocr`, `macshot://record`, etc. Trigger from Raycast, Alfred, BetterTouchTool, Shortcuts.</li> <li><b>Customizable toolbar background color</b> with adaptive light/dark appearance.</li> <li><b>`{random}` filename token</b> — 8-character base36 string per file in screenshot/recording filename templates.</li> <li><b>Per-glyph Stroke control</b> for the text tool, alongside Fill and Outline.</li> <li><b>11 new configurable overlay shortcuts</b> — Pin (defaults to F), Upload (U), Copy, Save, OCR, Scroll Capture, Beautify, Invert Colors, Remove Background, Translate, Undo, Redo. Configurable in Settings → Shortcuts.</li> <li><b>Keyboard navigation in the history overlay</b> — arrows, Enter/Cmd+C to copy, Cmd+E to open in editor, Space for Quick Look, Delete to remove.</li> <li><b>Dock right-click shows each window individually</b> — editor, video editor, and preferences windows each appear as their own menu item.</li> <li><b>"Open Video…" menu entry</b> — open a user-owned video in the editor for trimming, effects, and export without touching the source.</li> <li><b>Copy Screen Info</b> in Settings → About — copies display and capture diagnostics for bug reports.</li> <li><b>DMG drag-to-install layout</b> with arrow background, plus a "Move to Applications" prompt when launched from a translocated path.</li>  </ul><h3>Changed</h3><ul>  <li><b>Recording bitrate increased</b> so screen content stays crisp. New tiers target ~12 / ~22 / ~40 Mbit/s at 1440p30 for low/medium/high. B-frames disabled (standard tuning for screen content). Closes #140.</li> <li><b>Recording color space</b> is now forced to sRGB at capture time (macOS 14+), fixing washed-out playback on Display P3 screens.</li> <li><b>Faster first capture</b> — Core Animation/Metal pipeline pre-warmed at app launch; ScreenCaptureKit content cache no longer bypassed on every capture; instant transparent overlay while screenshots load in the background.</li> <li><b>Background windows no longer jump forward when triggering a capture</b> — editor/preferences/Sparkle windows stay behind the user's frontmost app.</li> <li><b>Clicking a history entry shows the floating thumbnail</b> for visual confirmation, instead of silently copying with no feedback. Closes #133.</li> <li><b>Sparkle update check interval reduced from 30 minutes to 24 hours</b> so update prompts don't reappear constantly during release windows.</li>  </ul><h3>Fixed</h3><ul>  <li><b>Wrong colors on external monitors</b> (editor + saved files) — captured screenshots are now normalized to sRGB at capture time. Some external monitors (e.g. ViewSonic VX4380) have ICC profiles that don't round-trip correctly through AppKit's rendering pipeline.</li> <li><b>Editor fits large images to the window on open.</b> Multi-megapixel images (e.g. 24MP photos via Photos.app "Edit in macshot") previously showed only the top-left corner. Closes #161.</li> <li><b>Clicking outside the selection no longer wipes annotation progress.</b> A recurring source of lost work — outside clicks are now a no-op when a selection already exists; ESC still cancels deliberately. Closes #154.</li> <li><b>Stroke width, line style, arrow style, shape fill, corner radius, and arrow Flip now persist globally</b> even when an annotation is selected. Refs #58.</li> <li><b>Tmp-file accumulation</b> — clipboard intermediates, share-sheet temps, cancelled recordings, and sandbox quarantine stubs all accumulated indefinitely. A new launch-time sweep keeps tmp at a few hundred KB steady state (was up to 1.2 GB). Closes #128.</li> <li><b>Settings window clipping</b> in Polish, German, Dutch, and other long-string locales. Closes #130.</li> <li><b>GIF export crash on longer recordings</b>, **GIF export freezes the UI**, and **GIF export shows no progress** — switched to a real background thread, fixed a use-after-free in finalize, added a percentage indicator.</li> <li><b>S3 and Google Drive image uploads ignored the filename template</b> — both now read the same `filenameTemplate` UserDefault that local saves use. Closes #147.</li> <li><b>Last used tool not persisted across app restarts</b> — selected annotation tool now survives via UserDefaults.</li> <li><b>Focus not returning to previous app</b> after capture, and **window snap highlight race condition** on the pre-screenshot transparent overlay.</li> <li><b>Color dithering in pinned images and editor window</b> — disabled `AutomaticAppKit` layer content format in favor of explicit `RGBA8` for pixel-perfect color reproduction.</li></ul>
    ]]></description>
    <enclosure
        url="https://github.com/sw33tLie/macshot/releases/download/v4.1.0/MacShot.dmg"
        length="5982790"
        type="application/octet-stream"
        sparkle:edSignature="3QqBso/j3tkFA41++NGFHfajPzpiy9SAkNvxQZiyrwVgQHWJUGdydhOigjCSNtv5OD/76iw1opdyHPMvFwCnAw=="
    />
</item>
    </channel>
</rss>
